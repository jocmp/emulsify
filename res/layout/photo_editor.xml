<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="match_parent">
    <ScrollView
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:id="@+id/pictureScrollView" android:layout_above="@+id/horizontalScrollView"
            android:layout_marginRight="5dp" android:background="#000000" android:fillViewport="false">
        <LinearLayout
                android:orientation="vertical"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent" android:id="@+id/pictureLinearLayout">
        </LinearLayout>
    </ScrollView>
    <ImageView
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:id="@+id/Picture" android:layout_above="@+id/horizontalScrollView"
            android:layout_toRightOf="@+id/pictureScrollView" android:background="#000000"/>
    <HorizontalScrollView
            android:layout_width="fill_parent"
            android:layout_height="@dimen/filterScrollHeight"
            android:id="@+id/horizontalScrollView" android:layout_alignParentEnd="false"
            android:layout_alignParentStart="false"
            android:background="#000000" android:layout_alignParentBottom="true" android:layout_alignParentLeft="true">
        <LinearLayout
                android:orientation="horizontal"
                android:layout_width="fill_parent"
                android:layout_height="fill_parent" android:id="@+id/linearLayout">
            <LinearLayout
                    android:orientation="vertical"
                    android:layout_width="fill_parent"
                    android:layout_height="fill_parent">
                <ImageView
                        android:layout_width="@dimen/filterImageWidth"
                        android:layout_height="@dimen/filterImageHeight"
                        android:id="@+id/imageView" android:src="@drawable/ic_launcher"/>
                <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:textAppearance="?android:attr/textAppearanceSmall"
                        android:text="Small Text"
                        android:id="@+id/textView" android:layout_below="@+id/imageView"
                        android:layout_gravity="center_horizontal"
                        android:layout_marginTop="@dimen/filterTextMargin"/>
            </LinearLayout>
        </LinearLayout>
    </HorizontalScrollView>
</RelativeLayout>